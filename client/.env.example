# HerSafety CI — Variables frontend
# Copier en .env pour le développement local

# URL de l'API backend (laisser vide si proxy Vite est actif)
VITE_API_URL=

# Mode de l'app (laisse vide ou 'production')
# Si vide : le TestBanner MODE TEST s'affiche
VITE_APP_MODE=development
